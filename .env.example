# Copy this file to .env and fill in real values. Do NOT commit .env.
# Generate JWT_SECRET_KEY with: openssl rand -base64 64

SQL_SA_PASSWORD=ChangeMe_StrongPassword123!
JWT_SECRET_KEY=replace-with-64-byte-base64-secret

SMTP_HOST=mailhog
SMTP_PORT=1025
SMTP_USERNAME=
SMTP_PASSWORD=
SMTP_FROM_EMAIL=noreply@egreetings.local
SMTP_FROM_NAME=eGreetings Dev
